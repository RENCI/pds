version: '3.4'
services:
  
  pdsaggregator:
    build:
      context: .
    restart: always
    environment:
      PDS_HOST: $PDS_HOST
      PDS_PORT: $PDS_PORT
      PDS_CONFIG: $PDS_CONFIG
    ports:
      - $API_PORT:8080

    
