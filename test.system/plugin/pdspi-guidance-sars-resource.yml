version: '3'
services:
  pdspi-guidance-sars-resource:
    image: txscience/pdspi-guidance-sars:${TX_TAG}
    port: 8080
    environment:
      SELECTOR_VALUE: "resource"
      PDS_HOST: ${PDS_HOST}
      PDS_PORT: ${PDS_PORT}
      PDS_VERSION: ${PDS_VERSION}
